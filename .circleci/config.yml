version: 2.0

jobs:
  basejob: &base-template
    docker:
      - image: nicolasbock:bml-ci
    steps:
      - run: apt-get update
      - run: apt-get install --assume-yes git-core
      - checkout

  build:
    <<: *base-template
    steps:
      - run: apt-get update
      - run: apt-get install --assume-yes git-core
      - checkout
      - run: ls -lh

workflows:
  version: 2
  build:
    jobs:
      - build
